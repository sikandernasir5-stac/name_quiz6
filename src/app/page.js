import Image from "next/image";
import styles from "./page.module.css";

export default function Home() {
  return (
  <mein styles={{color/:'black'}}>
  <div styles={{display:'flex',flexdirecton:'coloumn'}}                                               >
  {booking.map((b)=>(
   <div key={b.id}>
   <Image src >  




  );
}

